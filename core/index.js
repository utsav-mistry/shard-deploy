'use strict';

/**
 * core/ — Reserved for future pipeline modules.
 *
 * Planned:
 *   - health-check.js   — HTTP probe runner
 *   - pipeline.js       — Multi-stage deployment pipeline
 *   - registry.js       — Docker registry push support
 */

module.exports = {};
